from neo4j import GraphDatabaseimport jsonclass PokemonNeo4j:    def __init__(self, uri, user, password):        self.driver = GraphDatabase.driver(uri, auth=(user, password))    def close(self):        self.driver.close()    def insert_pokemon(self, pokemon_data):        with self.driver.session() as session:            for pokemon in pokemon_data:                session.run(                    """                    CREATE (p:Pokemon {name: $name, weight: $weight})                    """,                    name=pokemon["name"],                    weight=pokemon["weight"]                )if __name__ == "__main__":    uri = "bolt:    user = "neo4j"    password = "senha"      pokemon_file = "../output/pokedex_clean.json"    with open(pokemon_file, 'r') as f:        pokemon_data = json.load(f)    db = PokemonNeo4j(uri, user, password)    db.insert_pokemon(pokemon_data)    db.close()    print("Dados inseridos no Neo4j com sucesso!")